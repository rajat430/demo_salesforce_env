on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag name to rollback'
        required: true
        default: 'v1.0.0'
jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - name: script
        run: |
          echo "hello world"
          echo $GITHUB_REF_NAME
