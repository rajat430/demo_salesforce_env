name: "tmp workflow"
on:
  push:
    branches: main
jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - name: script
        run: |
          echo "hello world"
          echo $GITHUB_REF_NAME
