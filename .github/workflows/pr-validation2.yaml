name: "pr validation 2"
on:
  pull_request:
    branches: [develop,main]
jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - name: script
        run: |
          echo "hello world"
          echo $GITHUB_REF_NAME
